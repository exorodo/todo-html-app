## Why
Users need to remove todos they no longer need.

## What Changes
- Add delete button (x) inside each `<li>`
- CSS for delete button: right-aligned, transparent, red on hover
- JS: filter todo out by id on click, re-render

## Capabilities
### New Capabilities
- `todo-delete`: Delete button per todo item
### Modified Capabilities
## Impact
- Modifies `index.html`: HTML in renderTodos, CSS, JS handler
