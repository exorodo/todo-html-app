## Tasks
- [x] Style `#todo-list li` as `display: flex; align-items: center; justify-content: space-between;`
- [x] Add CSS for `.delete-btn`: small, no border, transparent bg, cursor pointer, color gray
- [x] Add CSS for `.delete-btn:hover`: color red
- [x] In `renderTodos()`: wrap text in `<span>`, add `<button class="delete-btn">x</button>`
- [x] Add `deleteTodo(id)` function: filter array, re-render
- [x] Bind delete button click with `e.stopPropagation()` to prevent toggle
