## Why
Users need to view subsets of their todos based on completion status.

## What Changes
- Add 3 filter buttons (All, Active, Completed) inside `#filters`
- Add `filter` variable and update `renderTodos()` to filter displayed items
- Highlight active filter button

## Capabilities
### New Capabilities
- `todo-filter`: Filter buttons to show all, active, or completed todos
### Modified Capabilities
## Impact
- Modifies `index.html`: HTML buttons, CSS for filters, JS filter logic
