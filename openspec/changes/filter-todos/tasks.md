## Tasks
- [x] Add 3 `<button>` elements inside `#filters`: All, Active, Completed with `data-filter` attribute
- [x] Style `#filters` as flex row with gap and margin-top
- [x] Style filter buttons: border, radius, bg transparent; `.active` with accent bg and white text
- [x] Add `let filter = 'all';` state variable
- [x] Add `setFilter(f)` function: update `filter`, update `.active` class on buttons, call `renderTodos()`
- [x] Update `renderTodos()` to filter todos array based on current filter before rendering
- [x] Bind click events on filter buttons to `setFilter()`
