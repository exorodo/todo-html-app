## Tasks

- [x] Create `index.html` with HTML5 doctype, lang, charset, viewport meta, and `<title>Todo App</title>`
- [x] Add inline `<style>` with CSS reset (`*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }`)
- [x] Define `:root` CSS custom properties: `--bg: #f5f5f5`, `--text: #1a1a1a`, `--accent: #2563eb`, `--border: #e0e0e0`, `--radius: 8px`
- [x] Style `body` with system font stack, `background: var(--bg)`, `color: var(--text)`, `min-height: 100vh`
- [x] Create `.app` container: `max-width: 600px`, `margin: 40px auto`, `padding: 0 20px`
- [x] Add `<h1>Todo App</h1>` styled with `text-align: center`, `color: var(--accent)`, `margin-bottom: 24px`
- [x] Add empty `<div id="input-area"></div>` placeholder
- [x] Add empty `<ul id="todo-list"></ul>` with `list-style: none` styling
- [x] Add empty `<div id="filters"></div>` placeholder
- [x] Add empty `<script>` tag at the end of body for future JS
