## Technical Approach
- Two helper functions: `saveTodos()` and `loadTodos()`
- `saveTodos()` called at end of `renderTodos()` (single save point after every mutation)
- `loadTodos()` called once at script init before first render
