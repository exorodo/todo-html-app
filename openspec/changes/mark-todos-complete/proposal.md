## Why
Users need to mark todos as done to track progress.

## What Changes
- Add click handler on each `<li>` to toggle `completed` state
- Add `.completed` CSS class with strikethrough and dimmed style
- Update `renderTodos()` to apply class based on `todo.completed`

## Capabilities
### New Capabilities
- `todo-toggle`: Click to toggle completed state with visual feedback

### Modified Capabilities

## Impact
- Modifies `index.html`: CSS class, JS click handler, renderTodos update
