## Tasks
- [x] Add CSS: `#todo-list li { cursor: pointer; transition: opacity 0.2s; }`
- [x] Add CSS: `#todo-list li.completed { text-decoration: line-through; opacity: 0.5; }`
- [x] Update `renderTodos()`: set `li.className` to `'completed'` if `todo.completed`
- [x] Add click listener on each `<li>`: toggle `todo.completed`, call `renderTodos()`
